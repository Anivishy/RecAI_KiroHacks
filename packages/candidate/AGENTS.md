# Candidate Package Ownership

- This package is the primary ownership area for the candidate lane.
- Read all shared Markdown docs before editing.
- Prefer editing only `packages/candidate/**` plus your own Markdown spec docs.
- Avoid changing recruiter-owned files unless the change is explicitly shared work.
