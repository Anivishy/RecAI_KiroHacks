# Recruiter Package Ownership

- This package is the primary ownership area for the recruiter lane.
- Read all shared Markdown docs before editing.
- Prefer editing only `packages/recruiter/**` plus your own Markdown spec docs.
- Avoid changing candidate-owned files unless the change is explicitly shared work.
