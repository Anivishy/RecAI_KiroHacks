# Shared Package Ownership

- This package is for code intentionally used by multiple lanes.
- Read the shared Markdown docs before editing.
- Prefer changing this package only when the change truly affects both recruiter and candidate implementations.
- If a change is shared, document it in a Markdown spec before or alongside the code.
